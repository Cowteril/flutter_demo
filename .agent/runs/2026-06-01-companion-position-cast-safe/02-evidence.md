# Evidence

- `SideActionBar` is positioned at `right: 8, bottom: 162` and includes the `施法` action at the bottom of the right column.
- `AiCompanionOverlay` previously reserved only `82` px for the right toolbar and allowed dragging close to the bottom controls.
- `_defaultCompanionPosition` used fixed magic offsets, placing the pet close to the cast action area.
