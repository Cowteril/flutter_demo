# Tester Task Card

Verify the model asset integration.

Run from `client/`:

- `dart format --output=none --set-exit-if-changed lib test`
- `flutter analyze`
- `flutter test`
- `flutter build apk --debug`

Expected output: exact commands, pass/fail result, warnings, and APK path if build succeeds.
